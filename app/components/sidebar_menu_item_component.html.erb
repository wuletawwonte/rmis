
<li>
    <a href="<%= to %>" class="group relative flex items-center gap-2.5 rounded-sm py-2 px-4 font-medium text-bodydark1 hover:bg-graydark dark:hover:bg-meta-4 <%= "bg-graydark dark:bg-meta-4" if current_page?(to) %>">
        <% if icon_name.present? %>
            <%= helpers.icon icon_name %>
        <% end %>

        <%= title %>
    </a>
</li>
