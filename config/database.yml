default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres

development:
  <<: *default
  database: rmis_development

test:
  <<: *default
  database: rmis_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
